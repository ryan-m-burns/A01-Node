extends layouts/main

block content
  div.about-container
    div.about-header
      img.headshot(src=about.headshot, alt=about.name)
      div.about-intro
        h1 About Me
        h2= about.title
    p.bio= about.bio
