extends ../layouts/main

block content
  div.project-detail-container
    h1= project.title
    div.project-detail-content
      div.project-detail-image
        img(src=project.screenshot, alt=`${project.title} Screenshot`)
      div.tech-stack
        h2 Tech Stack
        ul
          each tech in project.tech
            li= tech
      div.project-description
        h2 Description
        p= project.description
    button.back-btn(href="/projects") &larr; Back to Projects
    button.edit-btn(href=`/projects/${project.id}/edit`, onclick="event.stopPropagation()") Edit Project
    button.delete-btn(href=`/projects/${project.id}/delete`, onclick="event.stopPropagation()") Delete Project
  